{
  "watch": ["src", "public/locales"],
  "ext": "ts,json",
  "ignore": ["src/**/*.spec.ts"],
  "exec": "ts-node -r tsconfig-paths/register --project tsconfig.server.json src/server/index.ts",
  "env": {
    "NODE_ENV": "development",
    "NODE_OPTIONS": "--http-server-default-timeout=0"
  }
}
